generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model FlipHistory {
  id          String   @id @default(uuid())
  sessionId   String
  situation   String
  response    String
  createdAt   DateTime @default(now())
}
